name = "northfield-attack-path-proxy"
main = "worker.js"
compatibility_date = "2024-09-01"

# Secrets (never committed — set via `wrangler secret put`):
#   ES_URL      e.g. https://<your-project>.es.<region>.gcp.elastic.cloud
#   ES_API_KEY  the Elasticsearch API key (base64 id:api_key)
